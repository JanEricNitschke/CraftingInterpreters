var my_list = [1, 2, 3];
print(my_list); // expect: [1, 2, 3]

append(my_list, 4);
print(my_list); // expect: [1, 2, 3, 4]

append("Test", 5); // expect runtime error: 'Append' expects its first argument to be a list, got `Test` instead.